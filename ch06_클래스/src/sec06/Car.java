package sec06;

public class Car {
	
	//Field: 객체의 데이터가 저장되는 곳. 고유 데이터, 상태 데이터, 부품
	String company ="Hyundai"; 
	int speed;
	Tire tire = new Tire(); // 기본 타입이 아닌 경우 새로운 클래스를 만들어 준다.
	boolean navi = true;
	
	//Constructor(생성자): 객체를 생성할 때에만 사용함. 객체 안에는 생성자가 들어가지 않음.
	
	
	
	
	//Method
}
